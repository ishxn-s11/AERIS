# ml/notebooks

Reserved for exploratory notebooks. The reproducible pipeline lives in:

- `../training/generate_dataset.py` — SYNTHETIC dataset generation
- `../training/train_model.py` — model comparison, selection, artifact export

Notebook outputs are considered non-authoritative; the scripts are the source
of truth for model artifacts.
