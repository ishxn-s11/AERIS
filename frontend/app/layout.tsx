import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AERIS — Aerial & Industrial Environmental Risk Intelligence System",
  description:
    "IoT + AI hazardous-atmosphere monitoring and early warning for industrial facilities. Prototype decision-support system.",
};

export default function RootLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} bg-ink text-paper antialiased`}>
        {children}
      </body>
    </html>
  );
}
